let rows=[];const $=x=>document.getElementById(x);function esc(s){return String(s??"").replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]))}function render(){let q=$("q").value.toLowerCase(),r=$("r").value;let a=rows.filter(x=>(!q||JSON.stringify(x).toLowerCase().includes(q))&&(!r||x.recommendation===r));$("cards").innerHTML=a.slice(0,100).map(x=>`<article class="card"><div class="score">${x.score??0}</div><div><div class="title">${esc(x.title)}</div><div class="meta">${esc(x.source)} · ${esc(x.country)} · ${esc(x.notice_type)} · Deadline ${esc(x.deadline||"—")}</div><p>${esc(x.fit_reason)}</p><span class="badge">${esc(x.recommendation)}</span></div><a target="_blank" href="${esc(x.url||"#")}">Open ↗</a></article>`).join("")||"<p>No matches.</p>"}async function init(){ $("date").textContent=new Date().toLocaleDateString(undefined,{dateStyle:"long"});rows=await (await fetch("../data/opportunities.json")).json();$("n").textContent=rows.length;$("p").textContent=rows.filter(x=>x.recommendation==="PURSUE").length;$("i").textContent=rows.filter(x=>x.recommendation==="INVESTIGATE").length;$("u").textContent=rows.filter(x=>x.deadline&&((new Date(x.deadline)-new Date())/864e5)<=14).length;$("q").oninput=render;$("r").onchange=render;render()}init();